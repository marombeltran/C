/* day-005-samples -- source code for day-005
 * 2023 Free Software Foundation, Inc.
 */ 

#include <stdio.h>
#include <stdlib.h>

int
main (int argc, char *argv[])
{
    /* An operator is a special token that performs an operation, such as addition or subtraction,
     * on either one, two, or three operands.
     */
        2 + 2;
        ( 2 * ( ( 3 + 10 ) - ( 2 * 6 ) ) );

    exit (EXIT_SUCCESS);
}
 
